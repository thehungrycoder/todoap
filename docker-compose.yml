version: "2"
services:
  web:
    build: .
    ports:
      - "8090:8080"
    depends_on:
      - mongo
    volumes:
      - ./dist:/todoapp
  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - data-volume:/data/db

volumes:
  data-volume:
